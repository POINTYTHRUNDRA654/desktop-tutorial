name: CI

on:
  push:
    branches: [main, master, develop, feature/*, copilot/*]
  pull_request:
    branches: [main, master, develop]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  check-conflicts:
    name: Check for unresolved conflict markers
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          submodules: false

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24.x

      - name: Detect conflict markers
        run: node scripts/check-no-conflict-markers.js

  verify:
    name: Verify (reproducible)
    needs: check-conflicts
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          submodules: false
          lfs: true

      - name: Setup Node.js 24.x
        uses: actions/setup-node@v6
        with:
          node-version: 24.x
          cache: 'npm'

      - name: Cache Electron downloads
        uses: actions/cache@v4
        with:
          path: |
            ${{ runner.temp }}\electron-cache
            ${{ runner.temp }}\electron-builder-cache
          key: ${{ runner.os }}-electron-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-electron-

      - name: Configure npm for CI stability
        run: |
          npm config set fund false
          npm config set audit false
          npm config set fetch-retries 5
          npm config set fetch-retry-mintimeout 20000
          npm config set fetch-retry-maxtimeout 120000
          npm config set fetch-timeout 300000

      - name: Install dependencies (with retry)
        shell: pwsh
        run: |
          $retries = 3
          for ($i=1; $i -le $retries; $i++) {
            npm ci --prefer-offline --no-audit --no-fund
            if ($LASTEXITCODE -eq 0) { exit 0 }
            Write-Host "npm ci failed (attempt $i/$retries). Retrying in 15s..."
            Start-Sleep -Seconds 15
          }
          Write-Host "npm ci failed after $retries attempts."
          exit 1

      - name: Lint
        run: npm run lint

      - name: Unit tests
        run: npm run test:unit

      - name: IPC handler contract guardrail
        run: npm run test:ipc-contract

      - name: Build
        run: npm run build

  security-scan:
    name: Security Scan
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          submodules: false

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24.x
          cache: 'npm'

      - name: Configure npm for CI stability
        run: |
          npm config set fund false
          npm config set audit false
          npm config set fetch-retries 5
          npm config set fetch-retry-mintimeout 20000
          npm config set fetch-retry-maxtimeout 120000
          npm config set fetch-timeout 300000

      - name: Install dependencies
        run: npm ci --prefer-offline --no-audit --no-fund

      - name: Run npm audit
        run: |
          set +e
          npm audit --audit-level=moderate
          audit_exit_code=$?
          if [ "$audit_exit_code" -ne 0 ]; then
            echo "::warning title=Security Scan::npm audit reported vulnerabilities (exit code $audit_exit_code)."
          fi
          exit 0
