# Core Python Dependencies for Fallout 4 Blender Add-on
# These are the minimal dependencies needed for basic functionality
#
# VERSION NOTES
# -------------
# This file is used as a fallback when tool_installers.py cannot determine
# the running Python version.  tool_installers.install_python_requirements()
# applies tighter version pins automatically:
#
#   Blender 2.90-2.92 (Python 3.7): Pillow<10.0, numpy<2.0
#   Blender 2.93-3.0  (Python 3.9): Pillow>=9.0, numpy>=1.21
#   Blender 3.1+      (Python 3.10+): current releases below
#   Blender 4.x/5.x  (Python 3.11+): same + --break-system-packages
#
# If you are installing manually from a command line, use the per-version
# constraints above rather than this file directly.

# Image processing (Pillow 9.x is the last series with Python 3.7 wheels)
Pillow>=9.0.0

# Numerical operations (numpy 1.21 is the oldest wheel set supporting 3.7)
numpy>=1.21.0

# HTTP requests (for desktop tutorial app integration)
requests>=2.27.0

# 3D mesh processing (used by mesh helpers and image-to-mesh pipeline)
trimesh>=3.20.0

# PDF parsing for advisor knowledge ingestion
PyPDF2>=3.0.0

# Note: Blender includes many Python packages by default
# This file lists external dependencies that may need installation
