# Mossy v4.0 Environment Configuration Template
# DO NOT commit .env.local or any file with real API keys to GitHub.
# Copy this to .env.local and fill in your keys.

VITE_DEV_SERVER_PORT=5174

# Provider keys (MAIN PROCESS ONLY)
# Put these in `.env.local` for local dev, or configure them inside the Desktop app Settings UI.
# IMPORTANT: Do NOT use `VITE_*` for secrets. Vite exposes `VITE_*` vars to the renderer bundle.

OPENAI_API_KEY=
GROQ_API_KEY=
DEEPGRAM_API_KEY=
ELEVENLABS_API_KEY=

# TTS provider: 'browser' (default, no key needed) or 'elevenlabs' (requires key)
VITE_TTS_PROVIDER=browser
VITE_BROWSER_TTS_VOICE=Zira

# Neural Bridge token (internal security; change in production)
MOSSY_BRIDGE_TOKEN=change_this_to_a_long_random_string_in_production

# Voice detection thresholds (milliseconds)
VITE_SILENCE_WAIT_MS=350
VITE_MIN_SPEECH_MS=250
VITE_AMP_THRESHOLD=0.015

# Activity monitoring (optional)
VITE_ENABLE_ACTIVITY_MONITOR=false
VITE_LEARNING_LOG_RETENTION_DAYS=30
VITE_AUTO_SUGGESTION_THRESHOLD=0.75
