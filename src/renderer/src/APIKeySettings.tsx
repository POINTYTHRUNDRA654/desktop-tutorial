// APIKeySettings — removed. Mossy's AI backend handles all API keys server-side.
// Users do not configure or provide API keys. This file is intentionally empty.
import React from 'react';
const APIKeySettings: React.FC<{ embedded?: boolean }> = () => null;
export default APIKeySettings;
