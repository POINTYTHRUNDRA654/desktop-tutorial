# Optional Dependencies for AI/ML Features
# Install these only if you want to use AI-powered 3D generation features

# PyTorch - Required for most AI models
torch>=2.0.0
torchvision>=0.15.0

# Image processing for AI models
Pillow>=9.0.0

# Numerical operations
numpy>=1.21.0

# 3D processing
trimesh>=3.20.0

# HTTP/API interactions
requests>=2.27.0

# PDF parsing for advisor knowledge ingestion (optional)
PyPDF2>=3.0.0

# Gradio for web interfaces (optional)
gradio>=3.35.0

# Image processing for Real-ESRGAN upscaling
opencv-python>=4.7.0

# Motion capture / pose estimation (used by HyMotion helpers)
mediapipe>=0.10.0

# Diffusion models for Stable Diffusion pipelines
diffusers>=0.20.0

# Required by diffusers for model loading / mixed precision
accelerate>=0.20.0

# Real-ESRGAN super-resolution (basicsr + realesrgan + their face-enhancement deps)
basicsr>=1.4.2
realesrgan>=0.3.0
facexlib>=0.3.0
gfpgan>=1.3.8

# libigl mesh-processing library (imported as `igl`, used by RigNet helpers)
libigl>=2.4.1

# Speech-to-text (installed separately by the "Install Whisper" button but listed here
# so it can also be pulled in with the optional requirements file)
openai-whisper>=20231117

# OpenAI Shap-E and Point-E 3D generation models (GitHub-only packages).
# These are installed from source because they are not published on PyPI.
shap-e @ git+https://github.com/openai/shap-e.git
point-e @ git+https://github.com/openai/point-e.git
