# Brain B — Local Python/Gemma AI stack for Mossy
# Install with: pip install -r requirements.txt
# Requires Python 3.10+

# ── Core LLM inference (Unsloth + HuggingFace) ────────────────────────────────
unsloth[colab-new]>=2024.9.0
transformers>=4.43.0
accelerate>=0.33.0
bitsandbytes>=0.43.3          # 4-bit/8-bit quantization for Gemma on consumer GPUs
peft>=0.12.0                  # LoRA fine-tuning adapters
trl>=0.9.6                    # SFT / RLHF training loops

# ── Vector store & RAG ────────────────────────────────────────────────────────
chromadb>=0.5.0
sentence-transformers>=3.0.0  # BAAI/bge-small-en embeddings (512-dim, fast, free)
llama-index>=0.11.0
llama-index-retrievers-bm25>=0.3.0
rank_bm25>=0.2.2              # BM25 keyword search
# Result fusion uses Reciprocal Rank Fusion (RRF) — no external API key needed.
# RRF is implemented directly in gemma_service_enhanced.py.

# ── Agentic orchestration ─────────────────────────────────────────────────────
langgraph>=0.1.0
langchain>=0.2.0
langchain-community>=0.2.0

# ── Knowledge graph ──────────────────────────────────────────────────────────
networkx>=3.3

# ── Web search grounding (free, no API key required) ──────────────────────────
duckduckgo-search>=6.2.0      # DuckDuckGo HTML scraper — free, no API key

# ── Web framework (Flask agent API) ──────────────────────────────────────────
flask>=3.0.0
flask-cors>=4.0.0

# ── Database (episodic memory + feedback) ────────────────────────────────────
# SQLite is built-in to Python; no extra package needed.

# ── Utilities ────────────────────────────────────────────────────────────────
requests>=2.31.0
numpy>=1.26.0
torch>=2.3.0                  # Must match your CUDA version — see pytorch.org/get-started
torchvision>=0.18.0
tqdm>=4.66.0
psutil>=5.9.0

# ── All packages above are FREE and open-source. No API keys required. ────────
# Brain B requires a GPU (NVIDIA recommended, 8GB+ VRAM).
# Model weights download from HuggingFace on first run (free, ~5–15GB per model).
# DuckDuckGo search is rate-limited to ~100 queries/hour on the free tier.
